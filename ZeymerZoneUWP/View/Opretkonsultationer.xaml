<Page
    x:Class="ZeymerZoneUWP.Opretkonsultationer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ZeymerZoneUWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid Margin="0,0,6,546">
            <TextBlock Margin="0,0,290,193">Opret n Konsultation</TextBlock>
            <DatePicker Date="{x:Bind VejlederVM.NewKonDateDate, Mode=TwoWay}" x:Name="MyDatePicker" MinYear="{Binding MinYear}" MaxYear="{Binding MaxYear}" Margin="0,68,0,0" VerticalAlignment="Top" />
            <StackPanel Margin="0,103,130,0">
                <TextBox PlaceholderText="Timer" Text="{x:Bind VejlederVM.Timer, Mode=TwoWay}"/>
                <TextBox PlaceholderText="Minutter" Text="{x:Bind VejlederVM.Minutter, Mode=TwoWay}"/>
            </StackPanel>

        </Grid>
        <StackPanel Margin="0,402,6,10">
            <ListView ItemsSource="{x:Bind Path=VejlederVM.OC_Konsultationer}" Height="340">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local:Konsultation">
                        <StackPanel Orientation="Horizontal">
                            <TextBox IsReadOnly="True" Text="{x:Bind Mode=OneTime, Path=Konsultation_date}"/>
                            <TextBox IsReadOnly="True" Text="{x:Bind Mode=OneTime, Path=Kunde_Id}"/>
                            <TextBox IsReadOnly="True" Text="{x:Bind Mode=OneTime, Path=Vejleder_Id}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
        <Button ToolTipService.ToolTip="Klik her for at få vist Konsultationer" Content="Konsultationer" Height="32" Width="140" Command="{x:Bind VejlederVM.KonsultationKnap, Mode=OneWay}" Margin="286,352,0,0" VerticalAlignment="Top"/>
        <Button Content="Tilbage" Margin="362,10,0,0" VerticalAlignment="Top" Click="Button_Click"/>
        <Button Content="Opret" Margin="0,222,0,0" VerticalAlignment="Top" Command="{x:Bind VejlederVM.OpretKonsultation, Mode=OneWay}"/>
    </Grid>
</Page>
